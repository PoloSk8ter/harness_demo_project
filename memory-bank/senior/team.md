# Team roster — Senior only. Copy this to team.md (same folder) and replace the example devs.
# Only the Senior sees it — it lives in the Senior lane, and no role reads another's lane.
# The `split` skill assigns each task's Assignee from THIS list (no free-typed / ghost names).
# A dev's `name:` in their .claude/role must match their entry here, so `status --me <name>` works.
# Future: extend each line into a short portfolio (skills / past work) for compatibility-based
# assignment — AI-suggested, the Senior approves.
- amy: junior
- ben: junior
- chen: senior
